{% extends 'backend.html.twig' %}

{% block body %}
    <h1>Modifica titolo pagina</h1>

    {{ include('page/_form.html.twig', {'button_label': 'Salva'}) }}

    <a href="{{ path('app_page_index') }}">back to list</a>

{% endblock %}
