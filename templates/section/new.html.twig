{% extends 'backend.html.twig' %}

{% block body %}
    <h1>Create new Section</h1>

    {{ include('section/_form.html.twig') }}

    <a href="{{ path('app_section_index', {'id' : page.getId()}) }}">back to list</a>
{% endblock %}
